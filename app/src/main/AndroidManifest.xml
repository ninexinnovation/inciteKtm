<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ninexinnovation.incitektm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OverPricingActivity"
            android:label="@string/title_activity_over_pricing"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OverPricingProfile" />
        <activity android:name=".Rating" />
        <activity android:name=".OverPricingCreateProfile" />
        <activity android:name=".TravelFareActivity" />
        <activity android:name=".TravelFareLocal" />
        <activity android:name=".TravelFareTaxi" />
        <activity android:name=".FoodPricesActivity"></activity>
    </application>

</manifest>
